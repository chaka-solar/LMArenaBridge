# PostgreSQL 连接配置示例
# 复制此文件为 config.env 并填入实际值
# 使用方式: source config.env && ./run_all_batches.sh

# 数据库连接信息
export DB_HOST=localhost
export DB_PORT=5432
export DB_NAME=your_database_name
export DB_USER=postgres

# 数据库密码（注意安全）
export PGPASSWORD=your_password

# 输出目录
export OUTPUT_DIR=./results

# 批次配置（用于 Python 脚本）
export BATCH_SIZE=100

# 可选：SSL 连接
# export PGSSLMODE=require
# export PGSSLCERT=/path/to/client-cert.pem
# export PGSSLKEY=/path/to/client-key.pem
# export PGSSLROOTCERT=/path/to/ca-cert.pem

# 可选：连接超时
# export PGCONNECT_TIMEOUT=10

# 注意事项：
# 1. 不要将包含密码的 config.env 文件提交到版本控制系统
# 2. 建议使用 .pgpass 文件代替 PGPASSWORD 环境变量
# 3. 生产环境建议使用只读账户
